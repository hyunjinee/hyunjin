import * as t from '@babel/types'
import invariant from 'invariant'
import { LoggerEvent } from '../EntryPoint'

it('logs successful compilation', () => {
  const logs: [string | null, LoggerEvent[]] = []
})
