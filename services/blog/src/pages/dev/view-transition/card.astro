---
// app/[locale]/(ko-only)/dev/view-transition/card/page.tsx 이식 — metadata export 없음(dev/mcp.astro 선례),
// 페이지 전체가 React 실험 API 의존이라 client:only="react".
// biome-ignore-all lint/correctness/noUnusedImports: biome이 .astro 템플릿(--- 아래)을 파싱하지 않아 오탐
import ViewTransitionCard from '../../../components/islands/ViewTransitionCard'
import Base from '../../../layouts/Base.astro'
---

<Base locale="ko">
  <ViewTransitionCard client:only="react" />
</Base>
